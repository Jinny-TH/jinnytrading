# Jinny Trading v16 Exact Fix

반영 내용:

1. `오늘 시세 업데이트`는 현재가만 업데이트합니다. 배당률은 건드리지 않습니다.
2. 보유 종목 표의 현재가 입력 필드를 제거하고 평균단가처럼 텍스트로 표시합니다.
3. 보유 종목 각 행에 `수정` 버튼을 표시합니다. 수정 버튼을 누르면 설정 영역에 해당 종목 정보가 로드됩니다.
4. 동일 종목을 여러 계좌에 보유해도 `id` 기준으로 수정/삭제/시세 업데이트됩니다.

추가 SQL: 없음
